<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>

    <session-factory>
        <!-- Configurações do banco de dados -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/CondoFacil</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">123</property>

        <!-- Dialeto do PostgreSQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Mostrar SQL gerado -->
        <property name="hibernate.show_sql">true</property>

        <!-- Formatar SQL gerado -->
        <property name="hibernate.format_sql">true</property>

        <!-- Não criar o banco automaticamente -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Definir a classe de mapeamento -->
        <mapping class="com.seu.pacote.Apartamento"/>
        <mapping class="com.seu.pacote.Morador"/>
        <mapping class="com.seu.pacote.Sindico"/>
    </session-factory>

</hibernate-configuration>
